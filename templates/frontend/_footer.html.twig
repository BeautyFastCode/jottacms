
<div class="container">
    <div class="row">
        <div class="col-1">
            <a href="{{ path('app_frontend_homepage') }}">
                <div id="footer-logo" class="d-inline-block align-items-center" style="width: 50px; height: 50px;">
                    {{ source('@public_path'~asset('build/frontend/images/bfc-logo-eye.svg')) }}
                </div>
            </a>
        </div>
        <div class="col-11">
            <div class="text-end text-muted mb-2">
                <small>
                    <span>Bogumił Brzeziński | {{ 'phpDeveloper'|trans }} | © {{ 'now'|date('Y') }}</span>
                </small>
            </div>
            <div class="d-flex justify-content-end">
                {{ include('frontend/_socialmedia.html.twig') }}
            </div>
            <div class="text-end text-muted my-2">
                <small>
                    <a href="{{ path('app_admin_dashboard') }}">{{ 'dashboard'|trans }}</a>
                </small>
            </div>
        </div>
    </div>

</div>
